import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
  typography: {
    fontFamily: '"Roboto"',
    fontSize: 16
  },
  palette: {
    primary: {
      main: '#3f51b5'
    },
    secondary: {
      main: '#a0e3f3'
    }
  }
});
